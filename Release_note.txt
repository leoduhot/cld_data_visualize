v0.5.301
1. add supporting for malibu2 sensors
2. restruct data_parser and data_visualization utility

v0.5.220
1. change text formate for gen2 when on_click
2. reset matplotlib to default to fix color error when change between different projects
3. fix error when use signal filters for 05 project
4. align emg plot color and layout settings for all projects
5. change log, data and picture to log folder

v0.5.200
1. add project 05 for GEN2 devices

v0.5.101
1. add "gain" for EMG voltage calculate
2. fix file override due to tool too fast(timestamp would not update)
3. fix regenerate csv data from raw txt when press "Go"

v0.5.100
1. change 'tiki' to 'bali'
2. save raw&test data statistics data to csv
3. fix error when plot ceres test data

v0.5.000
1. save summary statistics in csv file
2. add limit entry for summary boxplot and histogram plot separately
3. change sensor type blank item to "others"
4. mark box plot and histogram as red if data out of limit
5. resort items to make baseline as the first one in plot and mark it as greed
6. mark cdf plot curve label to be red if data out of limit
7. support Ceres data(both raw data and tester data)
8. support Tiki data(both raw data and tester data)
9. support Tycho data(both raw data and tester data)

v0.4.201
1. add item filter
2. plot name rule: Plot Name -> Sensor Type -> File Name

v0.4.200
1. add CDF plot for summary data type
2. fix error when file content changed

v0.4.100
1. add feature to support selecting multiple files
2. add feature to support drag and drop folders and files
3. add feature to plot pictures for multiple files
4. fix error when sensor type is NULL

v0.4.000
1. change UI module from tkinter to QT (support drag and drop feature for both MacOS and Windows)
2. add checkbox for keeping parameters
3. add button for refreshing data columns
4. filter data columns for summary data by sensor type
5. auto resize plot texts according to window size
6. update raw data parser to support more un-consistency data format

v0.3.002:
1. Print outliers details(index, SN, test item, value) in log

v0.3.001:
1. Use scientific number for summary statistics info

v0.3.000:
1. Support P2 sensor raw data format
2. When parse raw data, generate css data file by default after select the "Raw Data" type

v0.2.021
1. fix parameter doesn't update when select MAG
2. [sensor summary data]compatible all other sensors
3. disable DND feature for Arm64 MacOS

v0.2.000 (only for Windows)
1. support raw sensor data(command output data)
2. add drag and drop feature for filepath
3. change APP icon
4. dynamic change checkbox height according to signal numbers
5. support tester data which don't have columns name(e.g.: emg single channel csv)


v0.1.003
1. add low pass filter implementation;
2. update UI layout to compatible with Windows PC;
3. fix callback args error

v0.1.002
1. apply filters to all sensors;
2. add error message box for errors

v0.1.001
1. use interactive backend: FigureCanvasTkAgg

v0.1.000
1. initialize